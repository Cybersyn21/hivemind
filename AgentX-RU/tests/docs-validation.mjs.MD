# 📚 Документация: docs-validation.mjs

## 🎯 Назначение файла

Этот файл представляет собой **тестовый скрипт для валидации документации** проекта Hive Mind. Он проверяет:

1. **Размер файлов документации** - чтобы они не превышали максимально допустимое количество строк
2. **Структуру README.md** - наличие обязательных разделов
3. **Качество документации** - соответствие стандартам проекта

Скрипт переписан с shell script в формат `.mjs` для кросс-платформенной совместимости и запускается как часть CI/CD pipeline.

---

## 📋 Полный код с подробными комментариями

```javascript
#!/usr/bin/env node
// ^^^^^^^^^^^^^^^^^^^
// Shebang - указывает системе использовать Node.js для выполнения этого скрипта
// Позволяет запускать файл напрямую как ./docs-validation.mjs

// =============================================================================
// ОПИСАНИЕ СКРИПТА
// =============================================================================
// Documentation validation test - rewritten from shell script to .mjs format
// Tests file size limits and README structure as required by CI
//
// Этот тест валидации документации был переписан с shell скрипта в .mjs формат
// Проверяет лимиты размера файлов и структуру README согласно требованиям CI

// =============================================================================
// ДИНАМИЧЕСКИЙ ИМПОРТ МОДУЛЕЙ ЧЕРЕЗ USE-M
// =============================================================================
// Use use-m to dynamically import modules for cross-runtime compatibility
// Используем use-m для динамического импорта модулей для кросс-runtime совместимости
//
// use-m - это утилита которая позволяет динамически загружать npm модули
// без необходимости package.json. Это полезно для:
// - Standalone скриптов
// - Запуска в разных окружениях (Node.js, Bun, Deno)
// - Избежания конфликтов зависимостей
const { use } = eval(await (await fetch('https://unpkg.com/use-m/use.js')).text());
// Что здесь происходит:
// 1. fetch('https://unpkg.com/use-m/use.js') - загружает use-m библиотеку с CDN
// 2. .text() - получает содержимое как текст
// 3. eval(await ...) - выполняет загруженный код
// 4. { use } - деструктурирует функцию use из результата

// =============================================================================
// ИМПОРТ НЕОБХОДИМЫХ МОДУЛЕЙ
// =============================================================================

// Модуль для работы с файловой системой (асинхронные операции)
const fs = (await use('fs')).promises;
// .promises - версия fs модуля с Promise-based API
// Позволяет использовать async/await вместо callbacks

// Модуль для работы с путями файлов
const path = (await use('path')).default;
// .default - получаем default export модуля path
// Предоставляет кросс-платформенные методы работы с путями

// Command-stream - библиотека для выполнения shell команд
const { $ } = await use('command-stream');
// $ - функция для выполнения shell команд с удобным синтаксисом
// (В этом скрипте импортирована, но не используется - возможно для будущего расширения)

// =============================================================================
// КОНФИГУРАЦИЯ
// =============================================================================

// Максимально допустимое количество строк в файлах документации
const MAX_LINES = 2500;
// ПОЧЕМУ 2500?
// - Файлы больше 2500 строк трудно читать и поддерживать
// - Большие файлы следует разбивать на под-документы
// - Это стандарт проекта для контроля качества документации

// Обязательные разделы которые должны присутствовать в README.md
const REQUIRED_SECTIONS = ['Quick Start', 'Architecture', 'Configuration'];
// ПОЧЕМУ ИМЕННО ЭТИ РАЗДЕЛЫ?
// - Quick Start: Пользователи должны быстро начать работу
// - Architecture: Разработчики должны понимать структуру проекта
// - Configuration: Важно знать как настроить проект

// Счетчик найденных ошибок
let errorsFound = 0;
// Используется для:
// - Подсчета всех ошибок валидации
// - Определения exit code (0 = успех, 1 = ошибки найдены)

// =============================================================================
// ВСПОМОГАТЕЛЬНАЯ ФУНКЦИЯ: ПОДСЧЕТ СТРОК В ФАЙЛЕ
// =============================================================================

/**
 * Асинхронно подсчитывает количество строк в файле
 *
 * @param {string} filePath - Путь к файлу для подсчета строк
 * @returns {Promise<number>} - Количество строк в файле
 */
const countLines = async (filePath) => {
  try {
    // Читаем файл как UTF-8 текст
    const content = await fs.readFile(filePath, 'utf8');
    // 'utf8' - кодировка, важно для правильного чтения международных символов

    // Разбиваем содержимое по символам новой строки и считаем количество
    return content.split('\n').length;
    // split('\n') - разделяет текст на массив строк
    // .length - возвращает количество элементов (строк)

  } catch (error) {
    // Если файл не может быть прочитан (не существует, нет прав доступа, и т.д.)
    console.error(`❌ ERROR: Could not read ${filePath}: ${error.message}`);
    // Выводим ошибку в stderr с emoji для наглядности

    return 0;
    // Возвращаем 0 чтобы не сломать дальнейшую логику
    // Фактическая ошибка будет обработана в вызывающем коде
  }
};

// =============================================================================
// ОСНОВНАЯ ФУНКЦИЯ 1: ПРОВЕРКА РАЗМЕРОВ ФАЙЛОВ
// =============================================================================

/**
 * Проверяет размеры файлов документации на соответствие лимитам
 * Проверяет:
 * 1. Все .md файлы в директории docs/
 * 2. Корневой файл README.md
 */
const checkFileSizes = async () => {
  console.log('📏 Checking documentation file size limits...');
  // Информируем пользователя о начале проверки

  // ---------------------------------------------------------------------------
  // ПРОВЕРКА ФАЙЛОВ В ДИРЕКТОРИИ docs/
  // ---------------------------------------------------------------------------
  try {
    const docsDir = 'docs';
    // Путь к директории с документацией (относительно текущей рабочей директории)

    // Проверяем существует ли директория docs/
    const docsStat = await fs.stat(docsDir);
    // fs.stat() - получает информацию о файле/директории
    // Бросает исключение если путь не существует

    if (docsStat.isDirectory()) {
      // Проверяем что это действительно директория, а не файл

      // Читаем список всех файлов в директории
      const files = await fs.readdir(docsDir);
      // readdir() - возвращает массив имен файлов (не полных путей)

      // Фильтруем только Markdown файлы
      const markdownFiles = files.filter(file => file.endsWith('.md'));
      // .endsWith('.md') - проверяет расширение файла
      // Игнорируем другие файлы (изображения, JSON, и т.д.)

      // Проверяем каждый Markdown файл
      for (const file of markdownFiles) {
        // Формируем полный путь к файлу
        const filePath = path.join(docsDir, file);
        // path.join() - безопасно объединяет части пути с правильными разделителями
        // Пример: 'docs' + 'README.md' = 'docs/README.md' (или docs\README.md на Windows)

        // Подсчитываем строки в файле
        const lineCount = await countLines(filePath);

        // Проверяем превышение лимита
        if (lineCount > MAX_LINES) {
          // ОШИБКА: Файл слишком большой
          console.log(`❌ ERROR: ${filePath} has ${lineCount} lines (max ${MAX_LINES})`);
          errorsFound++;
          // Увеличиваем счетчик ошибок для итогового отчета
        } else {
          // OK: Файл в пределах лимита
          console.log(`✅ OK: ${filePath} (${lineCount} lines)`);
        }
      }
    }
  } catch (error) {
    // Директория docs/ не существует или недоступна
    console.log('📁 No docs directory found, skipping...');
    // Это НЕ ошибка - директория docs/ опциональна
    // Некоторые проекты хранят документацию только в README.md
  }

  // ---------------------------------------------------------------------------
  // ПРОВЕРКА README.md
  // ---------------------------------------------------------------------------
  try {
    const readmePath = 'README.md';
    // Путь к главному README файлу (обязательный для всех проектов)

    // Проверяем существование README.md
    await fs.stat(readmePath);
    // Если файл не существует - бросится исключение, попадем в catch блок

    // Подсчитываем строки в README
    const readmeLines = await countLines(readmePath);

    // Проверяем превышение лимита
    if (readmeLines > MAX_LINES) {
      // ОШИБКА: README слишком большой
      console.log(`❌ ERROR: README.md has ${readmeLines} lines (max ${MAX_LINES})`);
      errorsFound++;
    } else {
      // OK: README в пределах лимита
      console.log(`✅ OK: README.md (${readmeLines} lines)`);
    }
  } catch (error) {
    // README.md не найден - это КРИТИЧЕСКАЯ ошибка
    console.log(`❌ ERROR: README.md not found: ${error.message}`);
    errorsFound++;
    // Каждый проект ДОЛЖЕН иметь README.md
  }
};

// =============================================================================
// ОСНОВНАЯ ФУНКЦИЯ 2: ПРОВЕРКА СТРУКТУРЫ README
// =============================================================================

/**
 * Проверяет наличие обязательных разделов в README.md
 * Разделы определены в REQUIRED_SECTIONS
 */
const checkReadmeStructure = async () => {
  console.log('📋 Checking README structure...');
  // Информируем о начале проверки структуры

  try {
    // Читаем полное содержимое README.md
    const content = await fs.readFile('README.md', 'utf8');
    // 'utf8' - важно для правильного чтения текста с разными языками

    // Проверяем каждый обязательный раздел
    for (const section of REQUIRED_SECTIONS) {
      // Простая проверка на наличие текста раздела в содержимом
      if (content.includes(section)) {
        // Раздел найден
        console.log(`✅ ${section} section found`);
        // ПРИМЕЧАНИЕ: Это простая проверка - только проверяем наличие текста
        // Не проверяем является ли это заголовком (# Quick Start)
        // Для более строгой проверки можно использовать регулярные выражения
      } else {
        // Раздел НЕ найден - это ошибка
        console.log(`❌ Missing ${section} section`);
        errorsFound++;
        // Без этих разделов документация считается неполной
      }
    }
  } catch (error) {
    // Не удалось прочитать README.md
    console.log(`❌ ERROR: Could not read README.md: ${error.message}`);
    errorsFound++;
    // Это критическая ошибка - без README нельзя проверить структуру
  }
};

// =============================================================================
// ГЛАВНАЯ ФУНКЦИЯ: ЗАПУСК ВСЕХ ТЕСТОВ
// =============================================================================

/**
 * Запускает все тесты валидации документации
 * Последовательность:
 * 1. Проверка размеров файлов
 * 2. Проверка структуры README
 * 3. Вывод итогового отчета
 * 4. Завершение процесса с правильным exit code
 */
const runTests = async () => {
  console.log('🚀 Starting documentation validation tests...');
  console.log(''); // Пустая строка для читаемости

  // ---------------------------------------------------------------------------
  // ВЫПОЛНЕНИЕ ТЕСТОВ
  // ---------------------------------------------------------------------------

  // Тест 1: Проверка размеров файлов
  await checkFileSizes();
  // await - ждем завершения всех проверок размеров

  // Тест 2: Проверка структуры README
  await checkReadmeStructure();
  // await - ждем завершения проверки структуры

  // ---------------------------------------------------------------------------
  // ИТОГОВЫЙ ОТЧЕТ
  // ---------------------------------------------------------------------------
  console.log('\n📊 Test Summary:');
  // \n - новая строка перед отчетом для отделения от логов тестов

  if (errorsFound === 0) {
    // ВСЕ ТЕСТЫ ПРОШЛИ УСПЕШНО
    console.log('🎉 All documentation tests passed!');
    process.exit(0);
    // Exit code 0 = успех
    // CI/CD системы интерпретируют это как успешное выполнение теста
  } else {
    // НАЙДЕНЫ ОШИБКИ
    console.log(`💥 Found ${errorsFound} errors in documentation validation`);
    console.log('📝 Split large files into subdirectories as required');
    // Подсказка пользователю как исправить ошибку

    process.exit(1);
    // Exit code 1 = ошибка
    // CI/CD системы интерпретируют это как провал теста
    // Pipeline остановится и не позволит смержить код с плохой документацией
  }
};

// =============================================================================
// ТОЧКА ВХОДА: АВТОМАТИЧЕСКИЙ ЗАПУСК
// =============================================================================

/**
 * Проверяем запущен ли этот скрипт напрямую (а не импортирован как модуль)
 *
 * import.meta.url - URL текущего модуля (например: file:///path/to/docs-validation.mjs)
 * process.argv[1] - путь к запущенному скрипту
 *
 * Если они совпадают - скрипт запущен напрямую, запускаем тесты
 * Если не совпадают - скрипт импортирован, не запускаем автоматически
 */
if (import.meta.url === `file://${process.argv[1]}`) {
  // Запускаем тесты
  runTests().catch(error => {
    // Ловим любые неожиданные ошибки во время выполнения тестов
    console.error(`❌ Test execution failed: ${error.message}`);
    // Выводим сообщение об ошибке

    process.exit(1);
    // Завершаем с кодом ошибки
    // Это гарантирует что CI/CD заметит проблему даже если она не в самих тестах
  });
}

// =============================================================================
// ЭКСПОРТ ФУНКЦИЙ ДЛЯ ИСПОЛЬЗОВАНИЯ КАК МОДУЛЬ
// =============================================================================

/**
 * Экспортируем основные функции чтобы их можно было использовать
 * в других тестах или скриптах
 *
 * Пример использования:
 * import { checkFileSizes } from './docs-validation.mjs';
 * await checkFileSizes();
 */
export { runTests, checkFileSizes, checkReadmeStructure };
```

---

## 🔍 Детальный разбор логики

### 1. Проверка размеров файлов (`checkFileSizes`)

**Что проверяется:**
- Все `.md` файлы в `docs/` директории
- Корневой `README.md`

**Логика проверки:**
```
ДЛЯ КАЖДОГО файла:
  1. Прочитать содержимое
  2. Разбить на строки (split('\n'))
  3. Посчитать количество строк
  4. ЕСЛИ строк > MAX_LINES (2500):
       - Вывести ошибку
       - errorsFound++
     ИНАЧЕ:
       - Вывести OK
```

**Зачем это нужно:**
- Предотвращает создание слишком больших файлов документации
- Заставляет разбивать большие документы на логические части
- Улучшает читаемость и поддерживаемость

### 2. Проверка структуры README (`checkReadmeStructure`)

**Что проверяется:**
- Наличие раздела "Quick Start"
- Наличие раздела "Architecture"
- Наличие раздела "Configuration"

**Логика проверки:**
```
Прочитать README.md
ДЛЯ КАЖДОГО обязательного раздела:
  ЕСЛИ content.includes(section):
    - Вывести "найден"
  ИНАЧЕ:
    - Вывести "отсутствует"
    - errorsFound++
```

**Примечание:** Проверка очень простая - только наличие текста. Не проверяет является ли это заголовком.

### 3. Итоговый отчет

**Exit Codes:**
- `0` - Все тесты прошли ✅
- `1` - Найдены ошибки ❌

**Важность для CI/CD:**
CI/CD pipeline проверяет exit code:
- Exit 0 → тест пройден → можно мержить PR
- Exit 1 → тест провален → нельзя мержить PR

---

## 🚀 Использование

### Запуск как standalone скрипт:
```bash
./docs-validation.mjs
# или
node docs-validation.mjs
```

### Импорт как модуль:
```javascript
import { checkFileSizes, checkReadmeStructure } from './docs-validation.mjs';

// Проверить только размеры файлов
await checkFileSizes();

// Проверить только структуру
await checkReadmeStructure();
```

---

## 📊 Примеры вывода

### Успешное выполнение:
```
🚀 Starting documentation validation tests...
📏 Checking documentation file size limits...
✅ OK: docs/CONFIG.md (226 lines)
✅ OK: docs/FLOW.md (461 lines)
✅ OK: README.md (672 lines)
📋 Checking README structure...
✅ Quick Start section found
✅ Architecture section found
✅ Configuration section found

📊 Test Summary:
🎉 All documentation tests passed!
```

### С ошибками:
```
🚀 Starting documentation validation tests...
📏 Checking documentation file size limits...
❌ ERROR: docs/HUGE_FILE.md has 3000 lines (max 2500)
✅ OK: README.md (672 lines)
📋 Checking README structure...
✅ Quick Start section found
❌ Missing Architecture section
✅ Configuration section found

📊 Test Summary:
💥 Found 2 errors in documentation validation
📝 Split large files into subdirectories as required
```

---

## 🛠️ Технические детали

### Используемые технологии:
- **Node.js** - среда выполнения
- **ES Modules** (`.mjs`) - модульная система
- **use-m** - динамическая загрузка модулей без package.json
- **Async/Await** - асинхронное программирование

### Зависимости:
- `fs.promises` - для асинхронной работы с файлами
- `path` - для работы с путями
- `command-stream` - импортирована но не используется

### Особенности:
1. **Кросс-платформенность** - работает на Windows, macOS, Linux
2. **Без package.json** - использует use-m для динамических импортов
3. **CI/CD ready** - правильные exit codes для автоматизации
4. **Модульность** - можно использовать как скрипт или библиотеку

---

## 🎯 Заключение

Этот файл является важной частью контроля качества документации проекта Hive Mind. Он:

✅ Автоматически проверяет размеры файлов документации
✅ Валидирует наличие обязательных разделов в README
✅ Интегрируется с CI/CD для автоматических проверок
✅ Предотвращает мерж кода с плохой документацией
✅ Поддерживает высокий стандарт качества проекта

**Запускается в:** CI/CD pipeline перед мержем pull requests
**Блокирует мерж:** Да, если тесты не пройдены
**Критичность:** Средняя (документация важна, но не блокирует функциональность)
