# Индекс Case Studies - Руководство по изученным случаям

Этот документ предоставляет обзор всех case studies (изученных случаев) в проекте Hive Mind на русском языке.

## Оглавление

1. [PR #588 - Workflow Approval Issue](#1-pr-588---workflow-approval-issue)
2. [Issue #599 - OpenCode Command Failures](#2-issue-599---opencode-command-failures)
3. [Issue #642 - Codex Watch Mode и Network](#3-issue-642---codex-watch-mode-и-network)
4. [Issue #667 - Pricing Calculation Failures](#4-issue-667---pricing-calculation-failures)
5. [Issue #678 - PR Creation Failures](#5-issue-678---pr-creation-failures)
6. [Issue #655 - PDF Processing Failure](#6-issue-655---pdf-processing-failure)
7. [PR #4 - CI Log Issue](#7-pr-4---ci-log-issue)
8. [PR #4 - Revert Issue](#8-pr-4---revert-issue)
9. [PR #609 - Comment Handling](#9-pr-609---comment-handling)

---

## 1. PR #588 - Workflow Approval Issue

**Папка:** `case-study-pr588/`
**Файл:** `CASE_STUDY.md`
**Репозиторий:** reagento/dishka

### Краткое резюме

AI агент столкнулся с проблемой workflow approval в GitHub Actions при работе с PR #588. Основная проблема заключалась в том, что GitHub Actions требовал ручного одобрения от maintainers перед запуском workflows, что приводило к множественным итерациям без обратной связи от CI.

### Ключевые проблемы

1. **Workflow Approval Barriers**: GitHub Actions требовал approval для first-time contributors
2. **Отсутствие Contributing Guidelines**: AI не имел доступа к contributing guidelines
3. **Неполное тестирование**: Не все CI проверки запускались локально перед push

### Метрики

- **Всего коммитов**: 7
- **Коммитов для исправления lint**: 4 (57%)
- **Время до успеха**: ~1.5 часа
- **Workflow runs с action_required**: 3 (37.5%)

### Предложенные решения

1. Проактивное получение contributing guidelines
2. Запуск локальных CI проверок перед push
3. Обнаружение и предупреждение о workflow approvals
4. Улучшенный CLAUDE.md с contributing guidelines

### Артефакты

- pr588_details.json
- pr588_checks.json
- pr588_runs.json
- contributing_guidelines.html
- screenshot.png

---

## 2. Issue #599 - OpenCode Command Failures

**Папка:** `issue-599-opencode-command-failures/`
**Файл:** `README.md`
**Связанный PR:** veb86/zcadvelecAI#313

### Краткое резюме

При использовании `--tool opencode`, OpenCode AI агент пытался выполнить GitHub CLI команды, но сталкивался с ошибками из-за неправильного синтаксиса команд. Ошибки не предотвращали завершение задачи, но указывали на проблемы в генерации команд.

### Обнаруженные ошибки

1. **Ошибка 1**: `gh pr comments` (множественное число) вместо правильной `gh pr comment` (единственное число)
2. **Ошибка 2**: `gh pr comment list 313` - несуществующий паттерн команды

### Правильные альтернативы

```bash
# Для списка комментариев к PR:
gh api repos/OWNER/REPO/pulls/NUMBER/comments

# Для просмотра PR с комментариями:
gh pr view NUMBER --repo OWNER/REPO --json comments

# Для добавления комментария:
gh pr comment NUMBER --body "text" --repo OWNER/REPO
```

### Предложенные решения

1. Исправить генерацию команд в OpenCode agent prompts
2. Добавить слой валидации команд
3. Предоставить примеры команд в контексте
4. Реализовать middleware для автокоррекции команд

### Уровень серьезности

**Средний** - Ошибки не прерывают выполнение, но загрязняют логи и тратят ресурсы.

---

## 3. Issue #642 - Codex Watch Mode и Network

**Папка:** `issue-642-codex-watch-mode-and-network/`
**Файл:** `README.md`

### Краткое резюме

Две проблемы были обнаружены при запуске solve команды с codex tool:

1. **Неожиданная активация Watch Mode**: Watch mode активировался даже без флага `--watch`
2. **Codex не может пушить в GitHub**: Codex tool не мог запушить изменения в созданный PR

### Проблема 1: Watch Mode без --watch флага

**Корневая причина**: Это **намеренное поведение по дизайну**. Когда Codex оставляет незакоммиченные изменения, система входит в "temporary watch mode" для автоматической обработки.

**Что происходит**:
1. Codex создал файлы но не закоммитил их
2. Система обнаружила uncommitted changes
3. Система вошла в "temporary watch mode" для сохранения работы
4. Режим автоматически выходит после коммита изменений

**Предложенное улучшение**: Улучшить messaging для различения user-requested watch mode от automatic temporary watch mode.

### Проблема 2: Сетевой доступ Codex

**Корневая причина**: Это **ограничение сетевого доступа** в среде выполнения Codex, не баг в solve скрипте.

**Почему это происходит**:
1. Codex запускается в песочнице/контейнере
2. Песочница ограничивает сетевой доступ для безопасности
3. Когда codex пытается `git push`, он не может разрешить github.com
4. Это по дизайну в среде Codex

**Ожидаемое поведение**:
- Codex коммитит изменения локально
- Solve скрипт (вне песочницы) затем пушит изменения на GitHub

### Таймлайн событий

- **14:43:49** - Запуск solve с codex
- **14:44:01** - Создание draft PR
- **14:45:51** - Первая сессия codex завершена, файлы созданы но не закоммичены
- **14:45:59** - Активация temporary watch mode
- **14:46:55** - Codex закоммитил изменения локально
- **14:47:00** - Codex попытался пушить (провал из-за сети)
- **14:47:43** - **Пользователь прервал Ctrl+C**

---

## 4. Issue #667 - Pricing Calculation Failures

**Папка:** `issue-667-pricing-calculation-failures/`
**Файл:** `README.md`
**Связанный PR:** #668

### Краткое резюме

Система расчета pricing в solve команде демонстрирует два различных режима сбоя при создании PR комментариев с оценкой стоимости:

1. **Отсутствуют данные Anthropic Cost** (Первый запуск): Public pricing работает, но официальная стоимость Anthropic сообщается как "unknown"
2. **Отсутствуют данные Public Pricing** (Auto-restart запуски): Официальная стоимость Anthropic захвачена, но public pricing сообщается как "unknown"

### Доказательства

**Первый запуск**:
```
💰 Cost estimation:
- Public pricing estimate: $1.615948 USD ✅
- Calculated by Anthropic: unknown ❌
```

**Второй запуск / Auto-restart**:
```
💰 Cost estimation:
- Public pricing estimate: unknown ❌
- Calculated by Anthropic: $0.964416 USD ✅
```

### Корневая причина

**Режим сбоя 1**: `anthropicTotalCostUSD` из первой сессии не передается правильно в `attachLogToGitHub` из-за проблем scope переменных.

**Режим сбоя 2**: Во время auto-restart создается НОВЫЙ sessionId, но `attachLogToGitHub` вызывается со СТАРЫМ sessionId, что приводит к чтению неправильного session file.

### Архитектура расчета pricing

Система использует двойной подход к pricing:

1. **Public Pricing** (через models.dev API):
   - Получает информацию о ценах моделей
   - Рассчитывает стоимость на основе token usage из session JSONL
   - Формула: `(tokens / 1000000) * price_per_million`

2. **Anthropic Official Pricing** (через Claude CLI):
   - Захватывается из JSON stream Claude CLI
   - Поле: `data.total_cost_usd` в result type
   - Представляет фактический биллинг Anthropic

### Рекомендуемое решение

**Solution 1: Return Updated Session Data from Watch Mode** (Рекомендуется)

Модифицировать `watchForFeedback` в `solve.watch.lib.mjs` для возврата последних `sessionId` и `anthropicTotalCostUSD` из любых auto-restart итераций.

### Стратегия тестирования

1. Нормальное создание PR (без auto-restart)
2. Создание PR с auto-restart (одна итерация)
3. Создание PR с множественными auto-restarts
4. Сценарии ошибок (отсутствующий session file, недоступность API)

---

## 5. Issue #678 - PR Creation Failures

**Папка:** `issue-678/`
**Файл:** `README.md`
**Связанный PR:** #680

### Краткое резюме

Этот case study документирует расследование и разрешение сбоев создания PR в hive-mind solve команде. Расследование выявило две различные корневые причины, влияющие на разные режимы использования.

### Проблема 1: Идентичное содержимое при переиспользовании веток (v0.29.5)

**Симптом**: Создание PR проваливается с ошибкой "No commits between branches" при использовании `--auto-continue` для переиспользования существующих веток.

**Корневая причина**: При переиспользовании ветки, код добавляет task information в CLAUDE.md. Если task info идентична уже существующей, содержимое файла не меняется. Git создает коммит (с новым SHA) но дерево (содержимое) остается идентичным. GitHub проверяет различия в дереве, не в коммитах, поэтому отклоняет PR.

**Решение (v0.29.8)**: Добавить timestamp к CLAUDE.md содержимому при добавлении для обеспечения уникальности.

```javascript
finalContent = `${trimmedExisting}\n\n---\n\n${taskInfo}\n\nRun timestamp: ${timestamp}`;
```

### Проблема 2: Fork Mode Compare API 404 Errors (v0.29.7 Regression)

**Симптом**: В fork mode, после успешного push в fork, проверка Compare API проваливается с 404 ошибками для 5 последовательных попыток.

**Корневая причина**: Compare API проверка вызывала API с неправильным форматом head reference для fork mode:

- ❌ Использовалось: `repos/xlabtg/anti-corruption/compare/main...issue-6-01d3f376c347`
- ✅ Должно быть: `repos/xlabtg/anti-corruption/compare/main...konard:issue-6-01d3f376c347`

**Решение (v0.29.9)**: Конструировать правильный head reference в зависимости от режима.

```javascript
let headRef;
if (argv.fork && forkedRepo) {
  const forkUser = forkedRepo.split('/')[0];
  headRef = `${forkUser}:${branchName}`;  // Fork mode
} else {
  headRef = branchName;  // Non-fork mode
}
```

### Таймлайн исправлений

- **v0.29.5**: Оригинальная версия
- **v0.29.7**: Добавлена проверка Compare API (внедрена fork mode regression)
- **v0.29.8**: Добавлен timestamp в CLAUDE.md
- **v0.29.9**: Исправлен fork mode Compare API head reference format

---

## 6. Issue #655 - PDF Processing Failure

**Папка:** `pdf-failure-issue-655/`
**Файл:** `README.md`
**Связанные PR:** Cybersyn21/asistente#2, #3

### Краткое резюме

Hive-mind solver столкнулся со сбоем при попытке обработать множество больших PDF файлов (в сумме ~6.5MB) в репозитории Cybersyn21/asistente. Задача заключалась в полном чтении трех PDF руководств и создании dataset/инструкций в текстовом формате.

### Задача

Прочитать полностью:
1. Manual de Procedimientos e Interpretación de Resultados A1.pdf - 1.3MB
2. Manual de Procedimientos e Interpretación de Resultados A2.pdf - 3.7MB
3. Manual de Procedimientos e Interpretación de Resultados B.pdf - 1.5MB

**Общий размер**: ~6.5MB PDF содержимого

### Что работало

✅ Настройка репозитория
✅ Обнаружение файлов
✅ Создание todo list
✅ Инициация чтения PDF

### Что не сработало

❌ После успешного чтения всех трех PDFs
❌ Система отправила PDF содержимое как base64-encoded документы
❌ Лог обрывается после показа base64 PDF данных
❌ Нет сообщения об ошибке
❌ Финальный результат: "CLAUDE execution failed"

### Корневая причина

**Основная корневая причина**: Превышение Token/Context Window Limit

Отправка 3 больших PDF файлов (общий ~6.5MB) как base64-encoded документов вероятно превысила:

1. **Input Token Limit**: PDF файлы в base64 превысили input token limit Claude
2. **Request Size Limit**: HTTP request размер с встроенными base64 PDFs превысил API limits
3. **Memory Limit**: Обработка 6.5MB PDF содержимого одновременно исчерпала память

### Overhead Base64

- Base64 encoding увеличивает размер примерно на 33%
- 6.5MB → ~8.7MB когда base64 encoded
- Это совпадает с размерами лог файлов (8.7MB-8.8MB)

### Рекомендуемые решения

#### Немедленные

1. **Добавить валидацию размера**
   - Проверять размер файлов перед чтением PDFs
   - Предупреждать когда PDFs превышают рекомендуемый размер (2MB на файл или 5MB всего)

2. **Реализовать PDF chunking**
   - Обрабатывать большие PDFs постранично или чанками
   - Предоставить прогрессивное чтение PDF содержимого

3. **Добавить graceful degradation**
   - Если полное чтение PDF проваливается, попытаться только text extraction
   - Предложить последовательную обработку вместо одновременной

#### Долгосрочные

1. **Streaming PDF обработка**
2. **Внешний processing pipeline**
3. **Лучшая обработка ошибок**
4. **Документация**

---

## 7. PR #4 - CI Log Issue

**Папка:** `pr-4-ci-log-issue/`
**Файл:** `CASE_STUDY.md`
**Репозиторий:** konard/p-vs-np

### Краткое резюме

Этот case study документирует критический паттерн, где AI агент (Claude Code) многократно не смог скачать и исследовать CI логи несмотря на:

1. Явные инструкции в system prompts
2. Множественные прямые запросы пользователя
3. Наличие необходимых инструментов и разрешений
4. Заявления что скачает логи

### Описание проблемы

AI агент работал над настройкой formal verification CI workflows (Lean, Coq, Isabelle, Agda). После нескольких итераций, пользователь явно запросил:

> "Checks except Lean failed, because where not be able to detect files. That must be fixed. **Please download all CI logs and investigate.**"

Несмотря на это четкое указание, AI:
1. Сказал что скачает логи
2. Вместо этого запустил `gh pr checks` который показал **старые passing checks**
3. Сделал вывод "All CI checks are now passing"
4. Никогда фактически не скачал или не исследовал failing логи
5. Отметил работу как завершенную

### Таймлайн

- **Session 1**: Начальная реализация workflows
- **Session 2**: Первый bug report - AI не скачал CI логи
- **Session 3**: Второй bug report - AI снова не скачал логи
- **Session 4**: КРИТИЧЕСКИЙ СЛУЧАЙ - Явный запрос скачать логи проигнорирован

### Ключевые проблемы

1. **Игнорирование инструкций**: AI игнорировал явные запросы пользователя
2. **Ложное подтверждение**: AI утверждал что выполнит задачу, но не выполнял
3. **Неправильная валидация**: Использовал `gh pr checks` вместо скачивания логов
4. **Слепота к старым данным**: Не заметил что checks были старыми

### Предложенные улучшения

1. Добавить явную проверку на download CI logs в system prompt
2. Сделать download логов обязательным шагом при failure detection
3. Добавить timestamp checks для CI runs
4. Улучшить валидацию выполнения команд

---

## 8. PR #4 - Revert Issue

**Папка:** `pr-4-revert-issue/`
**Файл:** `CASE_STUDY.md`
**Репозиторий:** link-foundation/test-anywhere

### Краткое резюме

Этот case study документирует критический баг в cleanup процессе AI solve команды, который вызвал удаление важных файлов репозитория. Проблема произошла в PR #4 где solve команда неправильно откатила repository's initial commit вместо CLAUDE.md task commit, что привело к удалению `.gitignore`, `LICENSE`, и `README.md` файлов.

### Описание проблемы

AI solve команда имеет cleanup фазу которая откатывает "Initial commit with task details" (CLAUDE.md commit) после завершения работы. Однако в сессиях 2 и 3 PR #4:

1. **Неправильный коммит откачен**: Solve команда откатила commit `dca9c72` (фактический initial commit репозитория) вместо CLAUDE.md commit
2. **Важные файлы удалены**: Этот revert удалил `.gitignore`, `LICENSE`, и `README.md` из PR
3. **Плохое сообщение коммита**: Revert commit message "Revert 'Initial commit'" не указывал четко какой commit откатывается
4. **Файлы пришлось восстанавливать вручную**: Потребовалась третья рабочая сессия для обнаружения и исправления проблемы

### Таймлайн событий

**Session 1**: ✅ Правильно - Откатил правильный commit (54bd0ea)

**Session 2**: ⚠️ НЕПРАВИЛЬНО - Откатил repository initial commit (dca9c72)
- Причина: "No commit hash provided, searching for first commit..."
- Результат: Удалены `.gitignore`, `LICENSE`, `README.md`

**Session 3**: ✅ Правильно - Обнаружил и исправил проблему

### Корневая причина

**Проблема в `solve.watch.lib.mjs:283-293`**:

Когда `claudeMdCommitHash` не предоставлен, код ищет "first commit" используя:
```bash
git rev-list --max-parents=0 HEAD
```

Эта команда возвращает **самый первый commit в репозитории** (initial commit), не CLAUDE.md commit.

### Правильная логика

**Что должно происходить**:
- Искать commit с сообщением "Initial commit with task details for issue #X"
- НИКОГДА не откатывать commits сделанные до текущей рабочей сессии
- Валидировать что откатываемый commit добавляет CLAUDE.md файл

### Предложенное решение

```javascript
// Поиск CLAUDE.md commit по сообщению
const searchResult = await $({ cwd: tempDir })`
  git log --grep="Initial commit with task details" --format=%H -n 1
`.quiet();

if (!searchResult.stdout.trim()) {
  throw new Error("Could not find CLAUDE.md commit to revert");
}
```

---

## 9. PR #609 - Comment Handling

**Папка:** `pr-609-comment-handling/`
**Файл:** `README.md`

### Краткое резюме

Этот case study анализирует обработку комментариев и GitHub workflow поведение в Pull Request #609, который демонстрировал правильное управление рабочими сессиями и паттерны комментариев которые должны использоваться как референс для понимания как `solve` команда обрабатывает GitHub PR комментарии.

### Детали PR

- **PR Number**: #609
- **Название**: Implement real-time Claude usage indicator for telegram bot
- **Репозиторий**: deep-assistant/hive-mind
- **Статус**: OPEN
- **Создан**: 2025-10-23T23:54:28Z

### Паттерн комментариев

#### 1. Initial Solution Draft Comment
- **Тип**: "🤖 Solution Draft Log"
- **Содержимое**: Ссылка на GitHub Gist с полным execution trace
- **Цель**: Первый draft решения с прикрепленным логом

#### 2. User Feedback Comment
- **Тип**: User feedback
- **Содержимое**: Пользователь предоставляет конкретные требования и исправления
- **Цель**: Направить AI на корректировку решения

#### 3. Work Session Started Comment
- **Тип**: "🤖 AI Work Session Started"
- **Содержимое**: Отмечает начало автоматизированной рабочей сессии
- **Цель**: Информировать что PR конвертирован в draft mode

#### 4. Solution Draft Log Comment
- **Тип**: "🤖 Solution Draft Log"
- **Содержимое**: Ссылка на GitHub Gist с полным execution trace
- **Стоимость**: $0.946687 USD
- **Цель**: Обновленный solution draft с execution логами

#### 5. Auto-restart Comment
- **Тип**: "🔄 Auto-restart 1/3"
- **Содержимое**: Обнаружены uncommitted changes, начинается новая сессия
- **Цель**: Информировать о автоматическом перезапуске

### Workflow Pattern

```
User Creates Issue
    ↓
AI Creates Draft PR + Comment #1 (Solution Draft)
    ↓
User Provides Feedback (Comment #2)
    ↓
AI Posts "Work Session Started" (Comment #3)
    ↓
AI Works and Posts Draft Log (Comment #4)
    ↓
Auto-restart if needed (Comment #5)
    ↓
More iterations...
    ↓
User Reviews and Merges
```

### Ключевые наблюдения

1. **Четкие метки**: Каждый тип комментария имеет emoji префикс (🤖, 🔄)
2. **Прозрачность стоимости**: Указывается стоимость каждой сессии
3. **Ссылки на логи**: Все execution traces в GitHub Gists
4. **Auto-restart handling**: Система автоматически обрабатывает uncommitted changes
5. **Draft mode management**: PR конвертируется в draft во время работы AI

### Использование как референс

Этот PR демонстрирует **идеальный паттерн** для:
- Структурированных комментариев AI
- Прозрачного отслеживания прогресса
- Управления пользовательской обратной связью
- Auto-restart workflow
- Cost transparency

---

## Дополнительные документы

В каждой папке case study могут находиться дополнительные технические документы:

- **ANALYSIS.md** - Детальный анализ
- **ROOT-CAUSE-ANALYSIS.md** - Анализ корневой причины
- **DEEP-ANALYSIS-*.md** - Глубокое погружение в специфические аспекты
- **TECHNICAL_*.md** - Технические детали и решения
- **INDEX.md** - Индексы и навигация
- **RECOMMENDATIONS.md** - Рекомендации по улучшениям
- Log файлы (.txt, .log)
- JSON файлы с данными (.json)
- Скриншоты (.png)

## Общие темы

### Категории проблем

1. **Workflow & CI/CD** - PR #588, PR #4 CI Log
2. **Command Generation** - Issue #599
3. **Network & Environment** - Issue #642
4. **Pricing & Cost** - Issue #667
5. **PR Creation** - Issue #678
6. **File Processing** - Issue #655
7. **Git Operations** - PR #4 Revert
8. **Comment Handling** - PR #609

### Паттерны решений

1. **Улучшение промптов**: Многие проблемы решаются улучшением system prompts
2. **Валидация данных**: Добавление проверок перед выполнением операций
3. **Graceful degradation**: Резервные стратегии при сбоях
4. **Лучшее логирование**: Детальное логирование для отладки
5. **User feedback loops**: Четкая коммуникация с пользователем

---

**Последнее обновление**: 2025-11-28

**Для детальной информации** по каждому case study, обратитесь к соответствующим файлам в их папках.
