# Анализ ошибок лимита скорости GitHub API

## Контекст Issue
**GitHub Issue**: [#186](https://github.com/deep-assistant/hive-mind/issues/186)
**Проблема**: Search API достигает лимитов скорости, нужен резервный вариант на repository listing API
**Цель**: Реализовать правильное обнаружение ошибок лимита скорости и механизм резервного варианта

## Ключевые находки

### 1. Паттерны ошибок лимита скорости

Когда команды GitHub CLI (`gh`) достигают лимитов скорости, они выдают специфические сообщения об ошибках, которые можно обнаружить:

#### Основная ошибка лимита скорости
```
HTTP 403: You have exceeded a secondary rate limit. Please wait a few minutes before you try again. If you reach out to GitHub Support for help, please include the request ID D84A:2DE4CB:565BC8C:5079198:68CB676D. (https://api.github.com/search/issues?advanced_search=true&page=3&per_page=100&q=is%3Aissue+type%3Aissue)
```

#### Паттерны обнаружения
Для обнаружения ошибок лимита скорости, проверяйте эти паттерны в сообщениях об ошибках:
- `"rate limit"` (без учета регистра)
- `"secondary rate limit"`
- `"HTTP 403"` в сочетании с терминами, связанными со скоростью
- `"exceeded.*limit"` (regex паттерн)
- `"Please wait"` или `"wait a few minutes"`
- `"abuse detection"` (механизм обнаружения злоупотреблений GitHub)
- `"too many requests"`

### 2. Различия в поведении API

#### Search API (`gh search issues`)
- **Лимит скорости**: 30 запросов в минуту
- **Вторичные лимиты скорости**: Триггерятся большими размерами страниц (>100) или быстрыми запросами
- **Лимит размера страницы**: Практический максимум ~100-200 элементов
- **Поведение**: Более строгие лимиты, более подвержен ограничению скорости
- **Случай использования**: Поиск по репозиториям, scope организации/пользователя

#### Repository Listing API (`gh issue list --repo`)
- **Лимит скорости**: Выше чем search API
- **Вторичные лимиты скорости**: Менее подвержен триггеру
- **Лимит размера страницы**: Может успешно обрабатывать 1000+ элементов
- **Поведение**: Более надежный, лучше для больших наборов данных
- **Случай использования**: Один репозиторий, стратегия резервного варианта

### 3. Максимальные размеры страниц

| Тип API | Рекомендуемый макс | Протестированный макс | Примечания |
|----------|-----------------|------------|-------|
| Search API | 100 | 200 | Более высокие значения триггерят вторичные лимиты скорости |
| Repository API | 1000 | 1000+ | Намного надежнее с большими размерами страниц |
| PR Listing | 1000 | 1000+ | Похоже на repository API |

### 4. Заголовки лимита скорости

Ответы GitHub API включают информацию о лимите скорости в заголовках:
```
X-Ratelimit-Limit: 30
X-Ratelimit-Remaining: 18
X-Ratelimit-Reset: 1758160590
X-Ratelimit-Resource: search
X-Ratelimit-Used: 12
```

## Стратегия реализации

### Текущая реализация
Кодовая база уже имеет функцию `fetchAllIssuesWithPagination()` в `/tmp/gh-issue-solver-1758160335449/github.lib.mjs` которая:
- Использует `execSync` для команд GitHub CLI
- Реализует пагинацию с улучшенными лимитами (1000)
- Добавляет задержки между запросами (5 секунд)
- Имеет базовую обработку ошибок

### Требуемые улучшения

1. **Добавить функцию обнаружения лимита скорости**
```javascript
function isRateLimitError(error) {
  const errorText = (error.stderr?.toString() || error.stdout?.toString() || error.message || '').toLowerCase();

  const rateLimitPatterns = [
    /rate limit/i,
    /secondary rate limit/i,
    /exceeded.*limit/i,
    /abuse detection/i,
    /too many requests/i,
    /please wait.*before/i,
    /wait.*(?:few )?minutes?/i,
    /http 403.*(?:rate|limit|abuse)/i
  ];

  return rateLimitPatterns.some(pattern => pattern.test(errorText));
}
```

2. **Реализовать стратегию резервного варианта**
```javascript
// В fetchAllIssuesWithPagination
try {
  const output = execSync(searchCommand, { encoding: 'utf8' });
  // ... обработка успеха
} catch (error) {
  if (isRateLimitError(error)) {
    await log('🚨 Обнаружен лимит скорости, переключение на repository listing API');
    return await fallbackToRepositoryListing(baseCommand);
  } else {
    // Обработать другие ошибки
    throw error;
  }
}
```

3. **Обновить стратегию размера страницы**
- Search API: Использовать максимум 100 элементов на запрос
- Repository API: Использовать до 1000 элементов на запрос
- Сохранить существующие 5-секундные задержки между запросами

### Логика резервного варианта

Когда search API достигает лимитов скорости:
1. Обнаружить ошибку лимита скорости используя паттерны выше
2. Парсить оригинальную команду для извлечения информации о репозитории
3. Конвертировать команду поиска в команду listing репозитория
4. Использовать более высокие лимиты страниц (1000) для repository API
5. Вернуть результаты в том же формате что и search API

### Пример конверсии команды

| Оригинал (Search API) | Резервный вариант (Repository API) |
|----------------------|---------------------------|
| `gh search issues org:microsoft is:open` | Множество вызовов `gh issue list --repo {repo} --state open` |
| `gh search issues user:username is:open` | Множество вызовов `gh issue list --repo {repo} --state open` |
| `gh search issues repo:owner/repo is:open` | `gh issue list --repo owner/repo --state open` |

## Файлы для модификации

### 1. `/tmp/gh-issue-solver-1758160335449/github.lib.mjs`
- Обновить функцию `fetchAllIssuesWithPagination()`
- Добавить обнаружение лимита скорости
- Реализовать логику резервного варианта
- Оптимизировать размеры страниц в зависимости от типа API

### 2. Точки использования
- `/tmp/gh-issue-solver-1758160335449/hive.mjs` (строки 585, 598, 629)
- Любые другие файлы, вызывающие `fetchAllIssuesWithPagination()`

## Стратегия тестирования

1. **Обнаружение лимита скорости**: Тест с намеренным триггером лимита скорости
2. **Механизм резервного варианта**: Проверить что repository API работает когда search проваливается
3. **Оптимизация размера страницы**: Тестировать разные лимиты для каждого API
4. **Обработка ошибок**: Обеспечить graceful degradation

## Преимущества

1. **Улучшенная надежность**: Резервный вариант обеспечивает что issues все еще могут быть получены
2. **Лучшая производительность**: Использовать оптимальные размеры страниц для каждого API
3. **Осведомленность о лимите скорости**: Правильное обнаружение и обработка
4. **Пользовательский опыт**: Graceful degradation вместо провалов

## Следующие шаги

1. Реализовать обнаружение лимита скорости в `fetchAllIssuesWithPagination()`
2. Добавить резервный вариант к repository listing API
3. Тестировать с различными сценариями лимита скорости
4. Обновить рекомендации размера страницы
5. Мониторить и логировать события лимита скорости для будущей оптимизации
