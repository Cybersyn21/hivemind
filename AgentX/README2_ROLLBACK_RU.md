# 🔄 Откат изменений и восстановление сервера Hive Mind

**Полное руководство по восстановлению работоспособности системы после неудачных изменений**

---

## 📑 Содержание

1. [Откат изменений в коде](#1-откат-изменений-в-коде)
2. [Перезагрузка компонентов системы](#2-перезагрузка-компонентов-системы)
3. [Очистка и восстановление дискового пространства](#3-очистка-и-восстановление-дискового-пространства)
4. [Полная переустановка Hive Mind](#4-полная-переустановка-hive-mind)
5. [Восстановление после критических ошибок](#5-восстановление-после-критических-ошибок)
6. [Бэкапы и точки восстановления](#6-бэкапы-и-точки-восстановления)
7. [Диагностика проблем](#7-диагностика-проблем)
8. [Чек-листы для быстрого восстановления](#8-чек-листы-для-быстрого-восстановления)

---

## 1. Откат изменений в коде

### 1.1 Проверка текущего состояния git

```bash
# Переключитесь на пользователя hive
su - hive

# Перейдите в директорию проекта
cd ~/hive-mind

# Посмотрите текущие изменения
git status

# Посмотрите последние коммиты
git log --oneline -10

# Посмотрите изменения в файлах
git diff
```

### 1.2 Откат незакоммиченных изменений

#### Вариант A: Откат всех изменений

```bash
# ВНИМАНИЕ: Это удалит ВСЕ незакоммиченные изменения!
git reset --hard HEAD

# Удалить все новые неотслеживаемые файлы
git clean -fd
```

#### Вариант B: Откат конкретного файла

```bash
# Откатить один файл к последней закоммиченной версии
git checkout HEAD -- src/gemini.lib.mjs

# Откатить несколько файлов
git checkout HEAD -- src/gemini.lib.mjs src/router.lib.mjs
```

#### Вариант C: Сохранить изменения на потом (stash)

```bash
# Сохранить текущие изменения во временное хранилище
git stash save "Неудачные изменения - сохранено для анализа"

# Посмотреть список сохранённых изменений
git stash list

# Восстановить изменения из stash (если понадобится)
git stash pop

# Удалить последний stash
git stash drop
```

### 1.3 Откат закоммиченных изменений

#### Вариант A: Откат последнего коммита (изменения остаются)

```bash
# Откатить последний коммит, но сохранить изменения в рабочей директории
git reset --soft HEAD~1

# Теперь можете отредактировать файлы и сделать новый коммит
```

#### Вариант B: Полный откат последнего коммита

```bash
# ВНИМАНИЕ: Это удалит последний коммит и все его изменения!
git reset --hard HEAD~1
```

#### Вариант C: Откат к конкретному коммиту

```bash
# Посмотреть историю коммитов с полными хешами
git log --oneline

# Откатиться к конкретному коммиту (замените HASH на нужный хеш)
git reset --hard abc1234

# Пример:
# git reset --hard 1a743d4
```

#### Вариант D: Создание revert коммита (безопасный откат)

```bash
# Создать новый коммит, отменяющий изменения предыдущего
# Это безопасно и сохраняет историю
git revert HEAD

# Revert конкретного коммита
git revert abc1234
```

### 1.4 Откат после git push

```bash
# ВНИМАНИЕ: Если вы уже запушили изменения, не используйте git reset!
# Вместо этого используйте git revert

# Откатить последний запушенный коммит
git revert HEAD
git push origin ваша-ветка

# Откатить несколько коммитов
git revert HEAD~2..HEAD
git push origin ваша-ветка
```

### 1.5 Синхронизация с upstream (основным репозиторием)

```bash
# Добавить upstream remote (если ещё не добавлен)
git remote add upstream https://github.com/deep-assistant/hive-mind.git

# Получить последние изменения из основного репозитория
git fetch upstream

# Посмотреть разницу между вашей веткой и upstream/main
git diff upstream/main

# Откатиться к состоянию upstream/main
git reset --hard upstream/main

# Принудительно запушить (ОСТОРОЖНО!)
git push -f origin ваша-ветка
```

---

## 2. Перезагрузка компонентов системы

### 2.1 Перезагрузка Telegram бота

#### Проверка состояния бота

```bash
# Посмотреть все screen сессии
screen -ls

# Подключиться к сессии бота
screen -r bot
```

#### Остановка бота

```bash
# Метод 1: Подключиться к screen и нажать Ctrl+C
screen -r bot
# Нажмите: CTRL + C

# Метод 2: Убить screen сессию извне
screen -S bot -X quit

# Метод 3: Найти процесс и убить его
ps aux | grep hive-telegram-bot
kill <PID>

# Метод 4: Убить все процессы hive-telegram-bot
pkill -f hive-telegram-bot
```

#### Запуск бота заново

```bash
# Создать новую screen сессию
screen -S bot

# Запустить бота с вашей конфигурацией
hive-telegram-bot --configuration "
  TELEGRAM_BOT_TOKEN: 'ваш_токен'
  TELEGRAM_ALLOWED_CHATS:
    -1002975819706
  TELEGRAM_BOT_VERBOSE: true
"

# Отсоединиться: CTRL + A, затем D
```

### 2.2 Перезагрузка активных solve/hive процессов

#### Остановка всех активных задач

```bash
# Посмотреть все screen сессии
screen -ls

# Остановить конкретную сессию
screen -S имя_сессии -X quit

# Остановить все сессии сразу
screen -ls | awk '/(Detached|Attached)/{print $1}' \
| while read s; do screen -S "$s" -X quit; done
```

#### Убить зависшие процессы

```bash
# Найти все процессы hive и solve
ps aux | grep -E '(hive|solve)\.mjs'

# Убить конкретный процесс
kill <PID>

# Убить процесс принудительно
kill -9 <PID>

# Убить все процессы solve
pkill -f solve.mjs

# Убить все процессы hive
pkill -f hive.mjs
```

### 2.3 Перезапуск Claude Code CLI

```bash
# Проверить работоспособность
claude --version

# Если Claude не работает, переавторизуйтесь
claude
# Следуйте инструкциям в браузере

# Проверить доступные профили
ls ~/.config/claude/profiles/

# Переключиться на другой профиль (если есть)
claude --profile другой_профиль
```

### 2.4 Перезагрузка всего сервера

#### Мягкая перезагрузка

```bash
# Под root пользователем
sudo reboot

# Или
sudo shutdown -r now
```

#### Перезагрузка с задержкой

```bash
# Перезагрузка через 5 минут с предупреждением
sudo shutdown -r +5 "Сервер будет перезагружен через 5 минут для обслуживания"

# Отменить запланированную перезагрузку
sudo shutdown -c
```

#### Жёсткая перезагрузка (из панели Contabo)

1. Зайдите в панель управления Contabo
2. Найдите ваш VPS
3. Нажмите "Restart" или "Reboot"
4. Подождите 2-5 минут для полной перезагрузки

#### После перезагрузки

```bash
# Подключитесь к серверу
ssh root@ваш_IP

# Переключитесь на пользователя hive
su - hive

# Перезапустите бота (если нужен)
screen -S bot
hive-telegram-bot --configuration "..."
# CTRL + A, D

# Проверьте что всё работает
claude --version
gh auth status
```

---

## 3. Очистка и восстановление дискового пространства

### 3.1 Проверка использования диска

```bash
# Общая информация
df -h

# Детальное использование по директориям
du -sh /* | sort -h

# Найти большие файлы (>100MB)
find / -type f -size +100M -exec ls -lh {} \; 2>/dev/null

# Топ-10 самых больших директорий
du -h /home/hive | sort -rh | head -10
```

### 3.2 Очистка временных файлов Hive Mind

```bash
# Удалить все временные директории solve
rm -rf /tmp/gh-issue-solver-*

# Удалить все временные директории hive
rm -rf /tmp/hive-*

# Очистить весь /tmp (ОСТОРОЖНО!)
rm -rf /tmp/*

# Очистить логи старше 7 дней
find /home/hive -name "*.log" -mtime +7 -delete

# Очистить логи старше 1 дня
find /home/hive -name "*.log" -mtime +1 -delete
```

### 3.3 Очистка системных временных файлов

```bash
# Под root пользователем
sudo -i

# Очистка /tmp
rm -rf /tmp/*

# Очистка /var/tmp
rm -rf /var/tmp/*

# Очистка apt кэша
apt-get clean
apt-get autoclean
apt-get autoremove

# Очистка journald логов (оставить только последние 3 дня)
journalctl --vacuum-time=3d

# Очистка старых ядер (оставить только текущее)
apt-get autoremove --purge
```

### 3.4 Очистка Node.js/npm кэша

```bash
# Переключитесь на hive пользователя
su - hive

# Очистить npm кэш
npm cache clean --force

# Очистить bun кэш
rm -rf ~/.bun/install/cache/*

# Посмотреть размер кэшей
du -sh ~/.npm ~/.bun/install/cache
```

### 3.5 Настройка автоматической очистки

Создайте cron job для автоматической очистки:

```bash
# Редактировать crontab
crontab -e

# Добавьте следующие строки:

# Очистка временных файлов Hive Mind каждый день в 3 часа ночи
0 3 * * * rm -rf /tmp/gh-issue-solver-* /tmp/hive-*

# Очистка старых логов каждую неделю
0 4 * * 0 find /home/hive -name "*.log" -mtime +7 -delete

# Сохраните и выйдите (в nano: Ctrl+X, Y, Enter)
```

---

## 4. Полная переустановка Hive Mind

### 4.1 Удаление текущей установки

```bash
# Переключитесь на hive пользователя
su - hive

# Остановите все процессы
screen -ls | awk '/(Detached|Attached)/{print $1}' \
| while read s; do screen -S "$s" -X quit; done

# Удалите глобальную установку
npm uninstall -g @deep-assistant/hive-mind

# Или для bun
bun remove -g @deep-assistant/hive-mind

# Очистите директорию
cd ~
rm -rf hive-mind
rm -rf node_modules
rm -f package.json package-lock.json
```

### 4.2 Чистая установка

```bash
# Установите заново
npm install -g @deep-assistant/hive-mind

# Или для bun
bun install -g @deep-assistant/hive-mind

# Проверьте версию
solve --version
hive --version

# Проверьте что всё работает
solve --help
```

### 4.3 Установка из исходников (для разработки)

```bash
# Клонируйте репозиторий
cd ~
git clone https://github.com/deep-assistant/hive-mind.git
cd hive-mind

# Установите зависимости
npm install

# Или для bun
bun install

# Сделайте скрипты исполняемыми
chmod +x src/*.mjs

# Создайте симлинки (опционально)
sudo ln -sf ~/hive-mind/src/solve.mjs /usr/local/bin/solve
sudo ln -sf ~/hive-mind/src/hive.mjs /usr/local/bin/hive
sudo ln -sf ~/hive-mind/src/telegram-bot.mjs /usr/local/bin/hive-telegram-bot
```

---

## 5. Восстановление после критических ошибок

### 5.1 Claude Code CLI не работает

```bash
# Проверить статус
claude --version

# Переустановить Claude Code CLI
npm uninstall -g @anthropic-ai/claude-code
npm install -g @anthropic-ai/claude-code

# Переавторизоваться
claude
```

### 5.2 GitHub CLI не работает

```bash
# Проверить статус
gh auth status

# Переавторизоваться
gh auth logout
gh auth login

# Выберите:
# - GitHub.com
# - HTTPS
# - Login with a web browser
```

### 5.3 Проблемы с правами доступа

```bash
# Под root пользователем
sudo -i

# Исправить владельца домашней директории hive
chown -R hive:hive /home/hive

# Исправить права на директории
find /home/hive -type d -exec chmod 755 {} \;

# Исправить права на файлы
find /home/hive -type f -exec chmod 644 {} \;

# Сделать скрипты исполняемыми
find /home/hive -name "*.mjs" -exec chmod +x {} \;
```

### 5.4 Проблемы с сетью/DNS

```bash
# Проверить доступность интернета
ping -c 4 8.8.8.8

# Проверить DNS
nslookup github.com

# Проверить доступность GitHub
curl -I https://github.com

# Проверить доступность Anthropic API
curl -I https://api.anthropic.com

# Перезапустить сеть (под root)
sudo systemctl restart networking
```

### 5.5 Система зависла / не отвечает

#### Через SSH

```bash
# Попробуйте подключиться через SSH
ssh root@ваш_IP

# Если подключение работает, посмотрите загрузку
top

# Найдите процессы, жрущие CPU/RAM
ps aux --sort=-%cpu | head -10
ps aux --sort=-%mem | head -10

# Убейте проблемные процессы
kill -9 <PID>
```

#### Через панель Contabo

1. Войдите в панель управления Contabo
2. Откройте VNC консоль (если доступна)
3. Попробуйте выполнить команды
4. Если не помогает - жёсткая перезагрузка

---

## 6. Бэкапы и точки восстановления

### 6.1 Создание бэкапа текущего состояния

```bash
# Переключитесь на hive пользователя
su - hive

# Создайте директорию для бэкапов
mkdir -p ~/backups

# Бэкап конфигурации
tar -czf ~/backups/config-$(date +%Y%m%d-%H%M%S).tar.gz \
  ~/.config/claude \
  ~/.config/gh \
  ~/.gitconfig

# Бэкап кода (если используете локальную разработку)
tar -czf ~/backups/hive-mind-$(date +%Y%m%d-%H%M%S).tar.gz \
  ~/hive-mind \
  --exclude=node_modules \
  --exclude=.git

# Бэкап логов
tar -czf ~/backups/logs-$(date +%Y%m%d-%H%M%S).tar.gz \
  ~/*.log

# Посмотреть созданные бэкапы
ls -lh ~/backups/
```

### 6.2 Восстановление из бэкапа

```bash
# Распаковать конфигурацию
cd ~
tar -xzf ~/backups/config-20250128-143022.tar.gz

# Распаковать код
tar -xzf ~/backups/hive-mind-20250128-143022.tar.gz

# Переустановить зависимости
cd ~/hive-mind
npm install
```

### 6.3 Автоматические бэкапы через cron

```bash
# Создайте скрипт бэкапа
cat > ~/backup.sh << 'EOF'
#!/bin/bash
BACKUP_DIR=~/backups
mkdir -p $BACKUP_DIR
DATE=$(date +%Y%m%d-%H%M%S)

# Бэкап конфигурации
tar -czf $BACKUP_DIR/config-$DATE.tar.gz \
  ~/.config/claude \
  ~/.config/gh \
  ~/.gitconfig

# Удалить бэкапы старше 7 дней
find $BACKUP_DIR -name "*.tar.gz" -mtime +7 -delete
EOF

# Сделайте скрипт исполняемым
chmod +x ~/backup.sh

# Добавьте в crontab
crontab -e

# Добавьте строку (бэкап каждый день в 2 часа ночи):
0 2 * * * ~/backup.sh
```

### 6.4 Экспорт бэкапов с сервера

```bash
# С вашего локального компьютера
scp -r hive@ваш_IP:~/backups ./local-backups/

# Или конкретный файл
scp hive@ваш_IP:~/backups/config-20250128-143022.tar.gz ./
```

---

## 7. Диагностика проблем

### 7.1 Проверка здоровья системы

```bash
#!/bin/bash
# Скрипт диагностики системы

echo "=== Диагностика Hive Mind ==="
echo

# Дисковое пространство
echo "1. Дисковое пространство:"
df -h | grep -E '(Filesystem|/$)'
echo

# RAM
echo "2. Использование RAM:"
free -h
echo

# CPU
echo "3. Загрузка CPU:"
uptime
echo

# Screen сессии
echo "4. Активные screen сессии:"
screen -ls
echo

# Процессы Hive Mind
echo "5. Процессы Hive Mind:"
ps aux | grep -E '(hive|solve|telegram-bot)\.mjs' | grep -v grep
echo

# Версии инструментов
echo "6. Версии установленных инструментов:"
echo "   Node.js: $(node --version 2>&1)"
echo "   npm: $(npm --version 2>&1)"
echo "   bun: $(bun --version 2>&1)"
echo "   gh: $(gh --version 2>&1 | head -1)"
echo "   claude: $(claude --version 2>&1)"
echo

# GitHub авторизация
echo "7. GitHub статус:"
gh auth status
echo

# Логи (последние 5 строк)
echo "8. Последние логи:"
tail -5 ~/*.log 2>/dev/null | head -20
echo

echo "=== Диагностика завершена ==="
```

Сохраните как `~/diagnose.sh` и запускайте:

```bash
chmod +x ~/diagnose.sh
~/diagnose.sh
```

### 7.2 Проверка логов на ошибки

```bash
# Найти ERROR в логах
grep -i "error" ~/*.log | tail -20

# Найти WARN в логах
grep -i "warn" ~/*.log | tail -20

# Найти последние 50 строк всех логов
tail -50 ~/*.log

# Следить за логами в реальном времени
tail -f ~/hive-*.log
```

### 7.3 Тестирование компонентов

```bash
# Тест Claude Code CLI
echo "Test prompt" | claude

# Тест GitHub CLI
gh repo view deep-assistant/hive-mind

# Тест Hive Mind (dry-run)
solve https://github.com/deep-assistant/hive-mind/issues/1 --dry-run

# Тест сети
curl -I https://api.anthropic.com
curl -I https://api.github.com
```

---

## 8. Чек-листы для быстрого восстановления

### 8.1 Чек-лист: Бот перестал отвечать

- [ ] Проверить screen сессии: `screen -ls`
- [ ] Подключиться к боту: `screen -r bot`
- [ ] Проверить логи на ошибки
- [ ] Перезапустить бота: убить сессию и создать новую
- [ ] Проверить токен бота в конфигурации
- [ ] Проверить Chat ID в allowed chats
- [ ] Протестировать команду `/help` в групповом чате

### 8.2 Чек-лист: Solve/Hive не работает

- [ ] Проверить версию: `solve --version`
- [ ] Проверить Claude: `claude --version`
- [ ] Проверить GitHub: `gh auth status`
- [ ] Проверить дисковое пространство: `df -h`
- [ ] Проверить RAM: `free -h`
- [ ] Очистить /tmp: `rm -rf /tmp/gh-issue-solver-*`
- [ ] Попробовать с --dry-run
- [ ] Посмотреть логи: `tail -f solve-*.log`

### 8.3 Чек-лист: Закончилось место на диске

- [ ] Проверить использование: `df -h`
- [ ] Найти большие файлы: `du -sh /* | sort -h`
- [ ] Удалить временные файлы Hive Mind: `rm -rf /tmp/gh-issue-solver-*`
- [ ] Очистить старые логи: `find ~ -name "*.log" -mtime +7 -delete`
- [ ] Очистить npm кэш: `npm cache clean --force`
- [ ] Очистить системный /tmp: `sudo rm -rf /tmp/*`
- [ ] Очистить journald: `sudo journalctl --vacuum-time=3d`
- [ ] Перезагрузить если нужно

### 8.4 Чек-лист: После обновления кода

- [ ] Создать бэкап текущего состояния
- [ ] Сделать git commit с описанием изменений
- [ ] Остановить все активные процессы
- [ ] Проверить синтаксис изменённых файлов
- [ ] Протестировать с --dry-run
- [ ] Запустить один тестовый issue
- [ ] Мониторить логи на ошибки
- [ ] Если работает - запустить бота
- [ ] Если НЕ работает - откатить через `git reset --hard HEAD~1`

### 8.5 Чек-лист: Полное восстановление с нуля

- [ ] Подключиться к серверу: `ssh root@IP`
- [ ] Переключиться на hive: `su - hive`
- [ ] Остановить все процессы: `screen -ls` → убить все
- [ ] Удалить установку: `npm uninstall -g @deep-assistant/hive-mind`
- [ ] Очистить временные файлы: `rm -rf /tmp/*`
- [ ] Переустановить: `npm install -g @deep-assistant/hive-mind`
- [ ] Переавторизовать Claude: `claude`
- [ ] Переавторизовать GitHub: `gh auth login`
- [ ] Протестировать: `solve --help`
- [ ] Запустить бота заново

---

## 🆘 Экстренная помощь

### Если ничего не помогает

**Вариант 1: Полная переустановка системы**

```bash
# Создайте бэкапы конфигурации
tar -czf ~/final-backup.tar.gz ~/.config ~/.gitconfig ~/*.log

# Скачайте бэкап на локальный компьютер
# scp hive@IP:~/final-backup.tar.gz ./

# Из панели Contabo выполните переустановку Ubuntu 24.04

# После переустановки запустите скрипт установки
curl -fsSL -o- https://github.com/deep-assistant/hive-mind/raw/refs/heads/main/scripts/ubuntu-24-server-install.sh | bash

# Восстановите конфигурацию из бэкапа
```

**Вариант 2: Обратиться за помощью**

1. GitHub Issues: https://github.com/deep-assistant/hive-mind/issues
2. Telegram канал: https://t.me/hive_mind_pull_requests
3. Создайте детальный отчёт:
   - Что пытались сделать
   - Какая ошибка произошла
   - Логи (`~/diagnose.sh`)
   - Что уже попробовали для решения

---

## 📝 Полезные команды для копирования

```bash
# Быстрая диагностика
df -h && free -h && screen -ls

# Остановка всего
screen -ls | awk '/(Detached|Attached)/{print $1}' | while read s; do screen -S "$s" -X quit; done

# Очистка всего
rm -rf /tmp/gh-issue-solver-* /tmp/hive-* && npm cache clean --force

# Откат git
git reset --hard HEAD && git clean -fd

# Перезагрузка
sudo reboot

# Бэкап конфигурации
tar -czf ~/backup-$(date +%Y%m%d-%H%M%S).tar.gz ~/.config ~/.gitconfig

# Полная переустановка Hive Mind
npm uninstall -g @deep-assistant/hive-mind && npm cache clean --force && npm install -g @deep-assistant/hive-mind
```

---

**Сохраните этот файл в закладки - он может спасти вас в критической ситуации! 🚨**

**Удачи в восстановлении! 💪**
